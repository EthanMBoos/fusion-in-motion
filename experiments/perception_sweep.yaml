# The scenario and question are in perception.yaml.
# Values in the same list position form one case.

name: Camera only, lidar only, and both
base_scenario: perception.yaml
seeds: [10, 11, 12, 13, 14]
parameter_combinations: zip
parameters:
  camera.enabled: [true, false, true]
  lidar.enabled: [false, true, true]
